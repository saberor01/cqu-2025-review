#include<iostream>
using namespace std;
int main(){
    int n,m;
    while(cin>>n>>m && (n!=0 && m!=0)){
        int add=0;
        int i;
        for(i=2;i<=n;++i){
            add=(add+m)%i; 
        }
        cout<<(add+1)<<endl;

    }
    return 0;
}
//也可以用单纯模拟法
